<div class="mshow-container">

  <div class="vignette text-center col-6">
    <% image_url = @mountain.photo_url.attached? ? @mountain&.photo_url : ‘’  %>
    <%= image_tag image_url %>
  </div>

  <div class="minfo1 col-5 ms-5">

    <h1 class="mt-3"><%=@mountain.name%></h1>
    <p>
      Located in the mountain range of <%= @mountain.range %>,
      in the country of <span><%= @mountain.location %></span>,
      <span><%=@mountain.name%></span> is <%= @mountain.height.floor.to_s.gsub(/\B(?=(...)*\b)/, ',') %> meters high.
      During your journey, you might encounter mostly <span><%= @mountain.terrain %></span>.
      There are <%= @mountain.trails %> trails  to follow, made for <span><%= @mountain.difficulty %></span>.
    </p>

    <div class="cards-show">
      <div class="card-show-info">
        <i class="fa-solid fa-earth-europe"></i>
        <p><%= @mountain.location %></p>
      </div>
      <div class="card-show-info text-center">
        <i class="fa-solid fa-mountain"></i>
        <p><%= @mountain.height.floor.to_s.gsub(/\B(?=(...)*\b)/, ',') %> meters</p>
      </div>
      <div class="card-show-info">
        <i class="fa-solid fa-shapes"></i>
        <p><%= @mountain.terrain %></p>
      </div>
      <div class="card-show-info">
        <i class="fa-solid fa-person-hiking"></i>
        <p><%= @mountain.difficulty %></p>
      </div>
    </div>

    <div class="card-show-price d-flex mt-3">
      <p class="me-2">Price :</p>
      <h4><%= @mountain.price.floor %> $<span>/night</span></h4>
    </div>

    <button type="button" class= "btn btn-info py-2 my-2">
      <h3><%= link_to "Rent #{@mountain.name} !", new_mountain_booking_path(@mountain) %></h3>
    </button>
  </div>
</div>
